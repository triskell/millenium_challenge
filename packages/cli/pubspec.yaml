name: cli
description: A command-line application to get the odds of getting to destination.
version: 1.0.0
publish_to: none

executables:
  give-me-the-odds:

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  core_backend: 
    path: ../core_backend

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
